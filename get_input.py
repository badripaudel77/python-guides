# Read input values from the console
# In python, we can get input values from user using built in function called input
# Syntax input("message"), that input value we can assign to the variable

name = input('What is your name ? ')
print("Hello " + name)
